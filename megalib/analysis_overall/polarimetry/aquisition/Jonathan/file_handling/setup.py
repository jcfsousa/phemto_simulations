from setuptools import setup

setup(
    name="file_handling",
    version="0.1.0",
    py_modules=["file_handling"],  # The name of your .py file without the extension
    install_requires=[],  # Add dependencies if needed
)
