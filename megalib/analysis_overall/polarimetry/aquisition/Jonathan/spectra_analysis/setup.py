from setuptools import setup

setup(
    name="spectra_analysis",
    version="0.1.0",
    py_modules=["spectra_analysis"],  # The name of your .py file without the extension
    install_requires=[],  # Add dependencies if needed
)
